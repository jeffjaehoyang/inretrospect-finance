{
  "dependencies": {
    "axios": "^0.24.0",
    "cors": "^2.8.5",
    "dotenv": "^10.0.0",
    "express": "^4.17.1",
    "fuse.js": "^6.4.6",
    "heroku-ssl-redirect": "^0.1.1",
    "redis": "^3.1.2",
    "yahoo-finance2": "^2.0.1"
  },
  "scripts": {
    "start": "node server.js",
    "heroku-postbuild": "cd client && npm install && npm run build:tailwind-prod && npm run build"
  },
  "engines": {
    "node": "16.3"
  },
  "homepage": "https://inretrospect-finance.herokuapp.com/",
  "devDependencies": {
    "prettier": "^2.5.1"
  }
}
